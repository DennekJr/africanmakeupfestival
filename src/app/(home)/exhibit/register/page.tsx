import * as React from "react";
import { SubPages } from "@/app/(home)/exhibit/register/SubPages";

const Register: () => Promise<React.JSX.Element> = async () => {
  return <SubPages />;
};

export default Register;
